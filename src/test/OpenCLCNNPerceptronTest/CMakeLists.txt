FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLCNNPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLCNNPerceptronTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(OpenCLCNNPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLCNNPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLCNNPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

