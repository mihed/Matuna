FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(Matuna.OCLDeviceTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(Matuna.OCLDeviceTest Matuna.OCLHelper)
SET_TARGET_PROPERTIES(Matuna.OCLDeviceTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(Matuna.OCLDeviceTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "Matuna.OCLDeviceTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

