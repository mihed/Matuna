FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(ZeroBorderKernelTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(ZeroBorderKernelTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(ZeroBorderKernelTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(ZeroBorderKernelTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "ZeroBorderKernelTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

