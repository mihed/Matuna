FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLCNNBackConvolutionTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLCNNBackConvolutionTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLCNNBackConvolutionTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLCNNBackConvolutionTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLCNNBackConvolutionTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

