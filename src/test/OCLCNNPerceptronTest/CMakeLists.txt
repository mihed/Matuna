FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLCNNPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLCNNPerceptronTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLCNNPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLCNNPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLCNNPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

