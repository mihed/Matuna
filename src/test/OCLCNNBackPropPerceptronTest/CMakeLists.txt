FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLCNNBackPropPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLCNNBackPropPerceptronTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLCNNBackPropPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLCNNBackPropPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLCNNBackPropPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

