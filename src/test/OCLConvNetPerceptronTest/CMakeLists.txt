FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLConvNetPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLConvNetPerceptronTest OCLConvNet ConvNet Math)
SET_TARGET_PROPERTIES(OCLConvNetPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLConvNetPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLConvNetPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

