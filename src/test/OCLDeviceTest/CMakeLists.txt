FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLDeviceTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLDeviceTest OCLHelper)
SET_TARGET_PROPERTIES(OCLDeviceTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLDeviceTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLDeviceTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

