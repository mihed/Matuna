FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLCNNBackPropPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLCNNBackPropPerceptronTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(OpenCLCNNBackPropPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLCNNBackPropPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLCNNBackPropPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

