FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(StandardOutputLayerTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(StandardOutputLayerTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(StandardOutputLayerTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(StandardOutputLayerTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "StandardOutputLayerTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

