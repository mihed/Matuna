FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLCNNBackConvolutionTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLCNNBackConvolutionTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(OpenCLCNNBackConvolutionTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLCNNBackConvolutionTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLCNNBackConvolutionTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

