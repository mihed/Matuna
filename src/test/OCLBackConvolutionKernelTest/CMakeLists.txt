FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLBackConvolutionKernelTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLBackConvolutionKernelTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLBackConvolutionKernelTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLBackConvolutionKernelTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLBackConvolutionKernelTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

