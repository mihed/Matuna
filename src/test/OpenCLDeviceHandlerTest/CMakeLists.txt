FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLDeviceHandlerTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLDeviceHandlerTest ${OpenCL_LIBRARY} OpenCLHelper)
SET_TARGET_PROPERTIES(OpenCLDeviceHandlerTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLDeviceHandlerTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLDeviceHandlerTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

