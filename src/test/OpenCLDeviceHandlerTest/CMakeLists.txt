FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLHelperTest ${sources_SRCS})
message(${OpenCL_LIBRARY})
TARGET_LINK_LIBRARIES(OpenCLHelperTest OpenCLHelper )
SET_TARGET_PROPERTIES(OpenCLHelperTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLHelperTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLHelperTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

