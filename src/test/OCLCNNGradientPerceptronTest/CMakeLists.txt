FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLCNNGradientPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLCNNGradientPerceptronTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLCNNGradientPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLCNNGradientPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLCNNGradientPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

