FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLCNNStandardOutputLayerTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLCNNStandardOutputLayerTest CNNOCL CNN Math)
SET_TARGET_PROPERTIES(OCLCNNStandardOutputLayerTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLCNNStandardOutputLayerTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLCNNStandardOutputLayerTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

