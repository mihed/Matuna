FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLDeviceTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLDeviceTest OpenCLHelper)
SET_TARGET_PROPERTIES(OpenCLDeviceTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLDeviceTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLDeviceTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

