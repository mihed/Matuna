FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OpenCLCNNGradientPerceptronTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OpenCLCNNGradientPerceptronTest CNNOpenCL CNN Math)
SET_TARGET_PROPERTIES(OpenCLCNNGradientPerceptronTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OpenCLCNNGradientPerceptronTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OpenCLCNNGradientPerceptronTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

