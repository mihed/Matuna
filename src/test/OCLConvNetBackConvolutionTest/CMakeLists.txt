FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLConvNetBackConvolutionTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLConvNetBackConvolutionTest OCLConvNet ConvNet Math)
SET_TARGET_PROPERTIES(OCLConvNetBackConvolutionTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLConvNetBackConvolutionTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLConvNetBackConvolutionTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

