FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLDeviceHandlerTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLDeviceHandlerTest OCLHelper)
SET_TARGET_PROPERTIES(OCLDeviceHandlerTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLDeviceHandlerTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLDeviceHandlerTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

