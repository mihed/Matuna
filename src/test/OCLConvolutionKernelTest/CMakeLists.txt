FILE (GLOB_RECURSE sources_SRCS *.cpp *.cxx *.cc *.C *.c *.h *.hpp)

ADD_EXECUTABLE(OCLConvolutionKernelTest ${sources_SRCS})
TARGET_LINK_LIBRARIES(OCLConvolutionKernelTest ConvNetOCL ConvNet Math)
SET_TARGET_PROPERTIES(OCLConvolutionKernelTest PROPERTIES LINKER_LANGUAGE CXX)
SET_TARGET_PROPERTIES(OCLConvolutionKernelTest PROPERTIES VERSION "${APPLICATION_VERSION_MAJOR}.${APPLICATION_VERSION_MINOR}" OUTPUT_NAME "OCLConvolutionKernelTest" CLEAN_DIRECT_OUTPUT 1)

#Install directives

